# ctuple
colors = ('red', 'green', 'blue', 2, 3, 4, 5, 9)

# accessing elements of the tuple
print(f'First element : {colors[0]}')
print(f'Last element : {colors[-1]}')

# length of the tuple
length = len(colors)

print(colors[-1:8])